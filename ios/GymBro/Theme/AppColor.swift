import SwiftUI

enum AppColor {
    static let background = Color(red: 0.06, green: 0.06, blue: 0.1)
    static let surface = Color(red: 0.12, green: 0.12, blue: 0.18)
    static let surfaceLight = Color(red: 0.18, green: 0.18, blue: 0.25)
    static let accent = Color(red: 0.4, green: 0.6, blue: 1.0)
    static let accentGreen = Color(red: 0.3, green: 0.9, blue: 0.5)
    static let accentOrange = Color(red: 1.0, green: 0.6, blue: 0.2)
    static let accentPurple = Color(red: 0.7, green: 0.4, blue: 1.0)
    static let textPrimary = Color.white
    static let textSecondary = Color(white: 0.6)
    static let error = Color(red: 1.0, green: 0.3, blue: 0.3)
    static let cardBorder = Color.white.opacity(0.08)
    static let glassBackground = Color.white.opacity(0.06)
}
